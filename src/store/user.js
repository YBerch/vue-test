import * as types from './actionTypes';

const state = {
  firstName: "",
  lastName: "",
  token: ""
};

const getters = {
};

const actions = {
};

const mutations = {
};

export default {
  state,
  getters,
  actions,
  mutations
};
